[project]
name = "sentinel-test"
version = "0.1.0"
description = "A simple FastAPI hello world service"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
